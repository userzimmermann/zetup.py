language: python
python:
  - '2.7'
  - '3.3'
  - '3.4'
  - pypy

install:
  - pip install -r requirements.txt --pre
  - pip install -r requirements.pytest.txt
  - pip install .

script:
  - python setup.py pytest
  - py.test pytest
